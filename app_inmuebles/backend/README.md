# Backend - App Inmuebles

Instalación y uso:

1. cd backend
2. npm install
3. npm run migrate
4. npm run dev
5. API en http://localhost:4000/api

La base de datos SQLite se genera en backend/data/db.sqlite